# OkiMAP 設定パネル説明

source: github-site
updated: 2026-04-27
category: site
repository: https://github.com/hirokawa-beach/OkiMAP
public_url: https://okimap.wplaceoki.com/

## 設定パネル

設定パネルは、画面左側の歯車ボタンから開きます。

設定を変更した場合は、最後に「設定を適用」ボタンを押してください。

変更が残っている場合、設定パネルはそのまま閉じられません。

## ズーム率きざみ

ズーム値を変更する細かさを設定します。

初期設定は `1` です。

`0.5` にすると、より細かく拡大・縮小できます。

通常は初期設定のままで問題ありません。

## ピクセル選択モード

ON にすると、クリックまたはタップしたピクセルの情報を確認できます。

色や座標を調べたいときに使います。

## ピクセルグリッドを表示

ON にすると、ズーム値8以上のときにピクセルの境界を表示します。

細かい確認には便利ですが、表示が重い場合は OFF にしてください。

## ピクセル色でオーバーレイ

ON にすると、ズーム値8以上のときにピクセルの色を重ねて表示します。

拡大時に色を見やすくするための設定です。

端末によって重くなる場合があります。

## サンプリング step

オーバーレイ表示で、どのくらい細かく色を表示するかを設定します。

初期設定は `1` です。

通常は変更する必要はありません。

値を大きくすると表示は粗くなります。

## 不透明度

オーバーレイの濃さを調整します。

画像が見づらい場合や、重ね表示を薄くしたい場合に変更します。

## 最小スクリーン間隔

開発者向けの調整項目です。

通常利用では変更する必要はありません。

## 先読み範囲

先読みで読み込む前後の範囲を設定します。

初期値は `30` です。

値を大きくすると、より広い範囲を先読みしますが、その分読み込み量も増えます。

## 設定を適用

設定の変更を反映するボタンです。

設定を変えた後は、必ず押してください。

## 元に戻す

設定を変更したあと、適用せずに前の設定へ戻すボタンです。

## 全体表示

沖ノ鳥島全体が画面内に収まるように表示を調整します。

PC では `r` キーでも全体表示できます。
